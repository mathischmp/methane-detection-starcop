from setuptools import setup, find_packages

setup(
    name="methan_detection",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Liste des dépendances de ton projet, par exemple :
        # 'numpy',
        # 'pandas',
    ],
)